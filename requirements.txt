# 코어 — GPU 머신 기준
torch>=2.6
transformers>=5.16.0      # Qwen3.5(qwen3_5) 지원 필요
accelerate>=1.0
peft>=0.14
datasets>=3.0
huggingface_hub>=0.30
pyyaml
numpy
pillow
decord               # 비디오 프레임 디코딩 (av 로 대체 가능)
einops

# CUT3R (기하 인코더) 의존성
scipy                # dust3r.utils.geometry
roma                 # 회전 유틸
omegaconf            # CUT3R 체크포인트 안의 학습 설정 객체 (torch>=2.6 안전 로딩)

# 평가
lmms-eval>=0.7.3     # vsibench / mmsi_bench / videomme 등. 자체 의존성을 많이 끌어온다

# 선택
flash-attn           # CUDA 전용. 맥에서는 설치하지 마라
wandb
