# 방향·구현 검증 — 2026-09-23

서버 피드백(Sensenova 전환)을 받은 시점의 점검. 기준 문서는 `DIRECTION_20260923.md`.

## 1. 구현 — 무엇이 증명됐고 무엇이 아직인가

### 증명됨 (로컬, 근거 있음)

| 항목 | 근거 |
|---|---|
| 비전 전처리 = 공식 | 공식 `patchify`·`smart_resize` 와 비트 단위 비교, 공식 프로세서 대비 max\|Δ\| 6e-8 |
| 프롬프트 = 공식 | 이미지·비디오 모두 공식 템플릿+프로세서와 **토큰 단위 일치**, `mm_token_type_ids` 일치 |
| thinking off | 빈 think 블록을 직접 넣는다 (템플릿 기본값이 0.8B·2B 는 off, **4B·9B 는 on** 이라 기본값에 기대면 안 된다 — 2026-09-24 실측). 감독 구간 = 답변+`<|im_end|>` 만 |
| 프로젝터가 학습됨 | 순수 zero-init 에서 기울기 흐름, 한 배치 과적합으로 손실 감소 (예전엔 기울기 0) |
| LoRA 가 DeltaNet 에 붙음 | `in_proj_qkv`·`in_proj_z` 포함 50모듈 (tiny), 없는 이름은 에러 |
| checkpointing 정합 | checkpointing 유무로 기울기 일치 (keep_primed), 없으면 깨짐도 확인 |
| DDP | 2프로세스 gloo, `no_sync` 누적 포함 **랭크 간 파라미터 비트 동일**, 검사기 음성대조 확인 |
| Sensenova 경로 | 문자열 `conversations`·멀티턴·크기 제각각 이미지·placeholder 불일치·누락 파일 처리 |
| 스트리밍 평가 조합 | 세션→세그먼트→질의→생성, 이미지·비디오 모드 모두 실제 토크나이저로, 감사 통과 |
| 저장소 무결성 | **깨끗한 클론**에서 테스트 78개 + 가짜 데이터 DDP 학습 통과 (클론 자신의 코드를 import 함을 확인) |

### 아직 (서버에서만 확인 가능)

| 항목 | 어디서 드러나나 |
|---|---|
| 서버 transformers 판이 같은 입력 형식을 만드나 | `NEXT_STEPS_SERVER` 2단계 (공식 대조 테스트) |
| Qwen3.5-4B 실가중치 로딩·메모리 | 4단계 |
| CUT3R 실가중치 hidden·격자·ms/frame | 5단계 |
| 학습 처리량 → 1에폭 시간 | 7단계 |
| **기하가 실제로 쓰이나** | 9단계 절제 평가 (S1 후) |
| lmms-eval 안에서 스트리밍 경로 실행 | 부품 조합은 검증, lmms 태스크 안 실행은 미검증 |
| **H100 에서의 지연 (라이브 주장의 근거)** | `bench_latency.py` 실모델 — 아직 한 번도 안 쟀다 |

## 2. 방향 — 발견한 문제 (우선순위순)

### ① [높음→중간] 학습 데이터가 바뀌어 73.3 과의 비교가 오염된다

> **2026-09-23 갱신 (사용자 답변)**: Sensenova 가 사내 시스템의 **기본 데이터셋**이고, 실험마다 다른
> 데이터가 추가로 붙는다. → 기본 데이터는 같으므로 우려가 크게 줄었다. 남는 건 73.3 을 찍은 실험에
> **무엇이 추가로 붙었는지**뿐이다 (그게 영상 데이터라면 ②와 연결된다).


목표 "라이브 70"은 사내 73.3 대비다. 그런데 학습 데이터가 (공간 부족으로) Sensenova 로 바뀌었다.
우리가 66 을 찍으면 그게 **스트리밍 비용**인지 **데이터 차이**인지 가를 수 없다.

- 스트리밍 평가의 `halving` vs `uniform_oracle` 차이는 **키프레임 선택 비용(손실②)**만 분리한다.
- **인코더 비용(손실① VGGT→스트리밍)**은 같은 데이터로 돌린 오프라인 VGGT 팔 없이는 분리 불가.

→ 필요한 정보: **73.3 의 학습 데이터에 Sensenova 가 들어 있나, 영상 데이터는 무엇이었나**
(`QUESTIONS_FOR_EXISTING_SYSTEM.md` P0-2). 답에 따라 VGGT 대조군을 앞당길지 정한다.

### ② [높음] 학습은 이미지, 주지표는 영상

Sensenova 는 레코드당 1~28장, 대부분 1~8장의 다시점 이미지다. VSI-Bench 는 1~2분 워크스루 영상이고
**개수 세기·방 크기·등장 순서·경로 계획**은 영상 전체를 통합해야 풀린다.
평가를 이미지 모드로 맞춰 입력 분포는 일치시켰지만, **32장짜리 긴 시퀀스를 학습에서 본 적이 없다.**

→ 영상 유래 학습 데이터가 필요하다. 공간이 문제라면 영상 대신 **프레임을 이미지 시퀀스로 저장**
(예: VSI-590K 어노테이션 + 씬당 32프레임 JPEG)하면 mp4 원본보다 훨씬 작다. 사용 가능한 영상 데이터가 뭔지 확인 필요.

### ③ [해결됨 2026-09-23] VideoMME 회귀 게이트 측정이 오염될 수 있다

> **해결**: `eval_path=live3r` (`src/live3r/eval/consistent.py`) — 이미지·영상 모두 학습과 같은 입력으로 평가.
> 조사해보니 부모 경로는 비디오 모드 말고도 **시스템 프롬프트**("You are a helpful assistant."),
> **해상도 규칙**(총 픽셀 예산)까지 세 군데가 달랐다. 또 qwen3_5 기본 **temperature 0.7** 때문에
> 온도를 안 정한 태스크(cv_bench)는 샘플링으로 돌고 있었다 → 기본을 greedy 로.
> 게이트 측정은 `scripts/run_gate.sh` + `check_gate.py` 로 자동화.
>
> **VideoMME 용량**: 101GB (+압축 해제 ≈ 200GB). 서버 여유가 1TB 라 **원래 게이트(VideoMME) 유지**
> (사용자 확인). MMStar(0.1GB)는 같은 실행에서 재서 보고만 한다.
> 판정 스크립트는 태스크별 대표 지표를 명시한다 — "첫 지표"를 쓰면 MMStar 는 하위 범주
> 하나로 판정될 뻔했다 (MMStar 는 `average` 가 맨 마지막).


채택 게이트 2번(VideoMME 회귀 ≤ 1.0)은 비스트리밍 lmms-eval 경로(`auto_geometry`)로 잰다.
이 경로는 영상을 **비디오 모드**(2프레임→1블록, 기하 2장 평균)로 넣는데, 프로젝터는 Sensenova 로
**이미지 모드만** 학습한다. 그러면 게이트는 "일반 능력 회귀"가 아니라 "프로젝터가 처음 보는 입력"을 잰다.

→ 게이트를 재기 전에 비스트리밍 평가도 이미지 모드 키프레임 경로로 통일해야 한다 (S2 전에 필요).

### ④ [중간] 최종 기하 상태가 LLM 에 안 들어간다 — 테제와 구현의 간극

테제: "LLM = 키프레임 비전 토큰 + **전 구간 기하 상태**". 구현: 키프레임 **시점의** 기하 토큰만 주입한다.
CUT3R 의 고정 크기 재귀 상태(**실제 체크포인트 768 토큰 × 768 차원**, 2026-09-24 실측 — 설정 기본값 324 가 아니다)는 질문 시점에 LLM 에 한 번도 노출되지 않는다.
키프레임 사이 프레임들은 **다음 키프레임 토큰을 통해 간접적으로만** 영향을 준다.

개수 세기·방 크기처럼 전 구간 통합이 필요한 문항, 그리고 OVO-S L4(allocentric)가 바로 이 부분이다.
**SpatialStack(VGGT) 대비 우리만의 차별점이 여기서 나온다** — 오프라인 VGGT 에는 "스트림 전체를 압축한
상태"라는 개념 자체가 없다.

→ S1 베이스라인 다음의 첫 아키텍처 실험 후보: 최종 상태 토큰을 풀링·투영해 **장면 메모리 토큰 ~64개**로
프롬프트에 넣기.

### ⑤ [중간] 해상도 예산이 73.3 과 다를 수 있다

지금 이미지당 최대 128×32×32 픽셀(≈362², LLM 토큰 ≤128개). 73.3 의 해상도는 모른다(질문 P0-6).
더 높았다면 스트리밍과 무관하게 점수를 잃는다. `--max-pixels` 로 조절 가능.

### ⑥ [중간] CUT3R 이 학습 병목일 수 있다

샘플당 이미지 5~8장을 fp32 CUT3R 로 인코딩한다. 스모크 B 의 `geom ms` 가 `fb ms` 보다 크면
bf16 추론(재구성 품질 확인 후)을 검토한다.

### ⑦ [낮음] 기타

- 멀티턴: 과거 턴에도 빈 think 블록을 넣는다(감독 토큰의 조건을 추론과 맞추려고). 공식 템플릿과 과거 턴 렌더링이 다르다.
- 인코더 베이크오프 미착수. Anchor3R·LingBot-Map 은 잠재 토큰 노출 방식이 문서화돼 있지 않아 연동 리스크가 있다.

## 3. 결정이 필요한 것

1. ~~73.3 의 데이터 구성~~ → Sensenova 가 기본 (답변 받음). 남은 것: 73.3 실험의 추가 데이터가 무엇인지
2. ~~VideoMME 게이트 경로 통일~~ → 완료 (`eval_path=live3r`)
3. **장면 메모리 토큰 실험 vs 인코더 베이크오프** → S1 결과를 보고 결정 (사용자 결정)
4. ~~일반 게이트 벤치~~ → VideoMME 유지 (서버 여유 1TB), MMStar 는 참고
